<div class="container">

  <h1>Cars Index Page</h1>

  <%= link_to 'My Favs', favourites_path %>

  <% @cars.each do |car| %>
  <p><%= link_to "#{car.brand} #{car.model}", car_path(car) %></p>
  <% end %>

</div>
